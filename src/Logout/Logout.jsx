import React from 'react'

const Logout = () => {
    const handleLogout = () => {
  logout();
  setIsLogin(false);
  navigate("/login");}
  return (
    <div>
      
    </div>
  )
}

export default Logout
